import { Flex } from '@chakra-ui/react'
import { Header } from '../../components/Header'

export const HomePage = () => {
  return (
    <Flex>
      <Header />
      <Flex>home page</Flex>
    </Flex>
  )
}
